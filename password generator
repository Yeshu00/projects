import string
import random
def passwordgenerator(length:int=10):
    selection=string.ascii_letters+string.digits+string.punctuation
    password="".join(random.choice(selection) for i in range (length))
    return password
you=passwordgenerator()
print(f"the password is{you}")
